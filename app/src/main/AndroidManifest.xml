<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fruit_sabzi_mandi">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FruitSabziMandi"
        android:windowSoftInputMode="adjustNothing">
        <activity android:name=".Farmer_Dashboard"></activity>
        <activity android:name=".Shop_Dashboard"></activity>
        <activity android:name=".DealDescription" />
        <activity android:name=".Shop_Details" />
        <activity android:name=".Signup" />
        <activity android:name=".Login" />
        <activity android:name=".Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>